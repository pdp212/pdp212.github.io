# Thư mục này chứa file video cục bộ (nếu không dùng YouTube/Vimeo).
#
# LƯU Ý QUAN TRỌNG:
# GitHub Pages có giới hạn dung lượng repo là 1GB.
# File video thường rất nặng (>50MB), nên KHUYẾN NGHỊ:
#   → Dùng YouTube hoặc Vimeo để host video (miễn phí)
#   → Chỉ lưu video ngắn <10MB tại đây nếu cần thiết
#
# NẾU VẪN MUỐN DÙNG VIDEO CỤC BỘ:
# Đặt file tại đây, sau đó trong data.js cập nhật:
#
#   showreel: {
#     embedUrl: null,   ← để null khi dùng video cục bộ
#     localVideoUrl: 'assets/videos/showreel-2024.mp4',
#   }
#
# Định dạng khuyến nghị: .mp4 (H.264), .webm
# Dung lượng tối đa: < 20MB mỗi file
